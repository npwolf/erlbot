%%% Default settings always get loaded
%%% Copy tuple's you wish to change to user_conf/user.conf.erl 

{channel_plugin, [
    % channels - List of channels to join on connect
    {channels, ["#erlang", "#bots"]}
]}.

